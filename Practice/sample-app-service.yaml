apiVersion: v1
kind: Service
metadata:
  name: sample-app-service
  labels:
    app: sample-app
spec:
  type: LoadBalancer
  selector:
    app: sample-app
    version: v1 # Initially send traffic to v1
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
